import { TopNav, SideNav, UIcard } from "../../components";

const MainUI = () => {
    return (
        <>
            <TopNav />
            <SideNav />
            <UIcard />
        </>
    );
};

export default MainUI;